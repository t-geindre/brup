#include "GlowingEdges.h"


/*
    #include <SFML/Graphics/Glsl.hpp>

    shipTexture.create(100, 100);
    shipShader.loadFromFile("../assets/shaders/gaussian_blur.frag", sf::Shader::Fragment);
    shipShader.setUniform("blur_radius", 0.02f);
    shipShader.setUniform("texture", sf::Shader::CurrentTexture);


    sf::Sprite sprite;
    sprite.setPosition(ship.getPosition());
    ship.setPosition(10, 10);

    shipTexture.clear(sf::Color(0,0,0, 0));
    shipTexture.draw(ship);
    shipTexture.display();
    sprite.setTexture(shipTexture.getTexture());

    target->draw(sprite, &shipShader);

    ship.setPosition(sprite.getPosition());


 */
